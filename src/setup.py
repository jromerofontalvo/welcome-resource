import setuptools

setuptools.setup(
    name="welcome-resource",
    version="0.1.0",
    py_modules=['welcome']
)

# setuptools.setup(
#     name="orquestra",
#     version="0.1",
#     packages=setuptools.find_packages(),
# )